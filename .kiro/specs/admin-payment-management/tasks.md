# План реализации: Управление платежами в админ-панели

## Overview

Расширение страницы `/admin/orders` для управления платежами: отображение статусов оплаты, выполнение capture (списание) и refund (возврат), просмотр истории и ошибок.

## Задачи

- [x] 1. Добавить CSS стили для статусов оплаты
  - Добавить классы `.payment-status`, `.payment-status-{status}`
  - Цветовая схема: pending (жёлтый), completed (зелёный), failed (красный), refunded (серый), partial_refunded (оранжевый)
  - Добавить стили для модальных окон деталей платежа
  - _Требования: 18.1_

- [x] 2. Добавить колонку "Оплата" в таблицу заказов
  - Добавить `<th>Оплата</th>` в `<thead>`
  - Добавить ячейку с статусом оплаты в каждую строку
  - Реализовать функцию `renderPaymentStatus(order)`
  - Обновить `renderOrders()` для включения новой колонки
  - _Требования: 18.1_

- [x] 3. Добавить фильтр по статусу оплаты
  - Добавить `<select id="filter-payment">` в панель фильтров
  - Опции: все, ожидает, оплачен, ошибка, возвращён, частичный возврат
  - Обновить `applyOrderFilters()` для нового фильтра
  - _Требования: 18.5_

- [x] 4. Реализовать детальную информацию о платеже
  - Добавить модальное окно `#payment-modal` в HTML
  - Реализовать `showPaymentDetails(orderId)` — загрузка и отображение
  - Показать: ID операции, статус, сумму, даты, способ оплаты, данные плательщика, чек
  - _Требования: 18.2_

- [x] 5. Реализовать историю платежа
  - Отобразить историю операций из `details.history`
  - Формат: время, действие, статус, сумма
  - Добавить в модальное окно деталей платежа
  - _Требования: 18.6_

- [x] 6. Реализовать отображение ошибок
  - Показать ошибки из `details.errors` (если есть)
  - Для каждой ошибки: дата, код, сообщение
  - _Требования: 18.7_

- [x] 7. Реализовать кнопку Capture (списание)
  - Добавить кнопку "Списать" в модальное окно
  - Показывать только для статусов `pending`, `authorized`
  - Реализовать `capturePayment(orderId, amount)` — вызов API
  - Добавить подтверждение перед списанием
  - Обновить таблицу после успешного capture
  - _Требования: 18.3_

- [x] 8. Реализовать кнопку Refund (возврат)
  - Добавить кнопку "Вернуть средства" в модальное окно
  - Показывать только для статусов `completed`, `captured`
  - Реализовать `refundPayment(orderId, amount, reason)` — вызов API
  - Проверка 90-дневного лимита с предупреждением
  - Поддержка частичного возврата (ввод суммы)
  - Обновить таблицу после успешного refund
  - _Требования: 18.4_

- [x] 9. Расширить экспорт CSV
  - Добавить колонки: статус оплаты, сумма оплаты, дата оплаты
  - Применить текущие фильтры к экспорту
  - Обновить `exportOrders()`
  - _Требования: 18.8_

- [x] 10. Контрольная точка
  - Проверить все функции
  - Убедиться, что фильтры работают корректно
  - Проверить API вызовы

## Notes

- Используется существующий API из tochka-payment-integration
- Минимальные изменения — только frontend (orders.html)
- Обратная совместимость: для заказов без payment_operation_id показывать "—"